# sergio-brain (engine)

Python memory engine for SERGIO BRAIN. See the repository root `README.md`, `INSTALL.md` and `ARCHITECTURE.md`.

```bash
pip install -e ".[local,docs,dev]"
sergio-brain doctor
```
